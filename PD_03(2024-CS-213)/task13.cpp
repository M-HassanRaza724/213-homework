# include <iostream>
using namespace std;

int main()
{
    int input;
    int sum = 0;

    cout<<"Enter number 1: ";
    cin>>input;
    sum = sum + input;

    cout<<"Enter number 2: ";
    cin>>input;
    sum = sum + input;
    
    cout<<"Enter number 3: ";
    cin>>input;
    sum = sum + input;
    
    cout<<"Enter number 4: ";
    cin>>input;
    sum = sum + input;
    
    cout<<"Enter number 5: ";
    cin>>input;
    sum = sum + input;
    
    cout<<"Sum of entered five numbers is: "<<sum;
    
    return 0;
}
